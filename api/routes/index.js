const usersRouter = require("./usersRouter");
const authRouter = require("./authRouter");
const sessionsRouter = require("./sessionRouter");

module.exports = { usersRouter, sessionsRouter, authRouter };
